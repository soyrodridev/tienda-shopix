/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: [
      "fakestoreapi.com",
      "images.pexels.com"
    ],
  },
};

module.exports = nextConfig
